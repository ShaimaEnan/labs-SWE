
package arrays;

import arrays.arrays;
import java.util.Arrays;


public class main {

    public static void main(String[] args) {
     arrays arr=new arrays();
     int []numbers={1,2,4,5,61,7,9,10,11};
    //Arrays.sort(numbers);
    arr.shifright_insert(numbers, 8,2,99);
    arr.Traversal(numbers);
     /*int result=arr.binnary_search(numbers,81,0,numbers.length-1);
     if(result !=-1){
        System.out.println("value found at index"+result);}
     else{
         System.out.println("value not found");}*/
     
     
     
     //numbers[3]=15;
     //arr.update(numbers, 3, 15);
     //arr.delete(numbers, 4);
     //int []numbers2=new int[5];
     //arr.input(numbers2);
     //arr.R_Traversal(numbers);
     //arr.Traversal(numbers);
     //shiftright_insrtتكليف
     
     
     
  
     
    }

}
